{
  "name": "@llm-series/retriever",
  "version": "0.1.0",
  "description": "RAG retriever combining chunker, embedder, and vector-store-lite — module #8b",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm run build && pnpm run typecheck"
  },
  "files": ["dist"],
  "dependencies": {
    "@llm-series/chunker": "workspace:*",
    "@llm-series/embedder": "workspace:*",
    "@llm-series/vector-store-lite": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.2"
  }
}
